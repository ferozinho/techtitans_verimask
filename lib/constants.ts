export const AGE_THRESHOLD = 18;
export const GPA_TENTHS_THRESHOLD = 35;
export const PRESENTATION_TTL_MS = 15 * 60 * 1000;
export const SESSION_TTL_SECONDS = 20 * 60;
export const PICKUP_TTL_SECONDS = 15 * 60;

export const STORAGE = {
  issuer: "verimask.issuer",
  holder: "verimask.holder",
  kiosk: "verimask.kiosk",
} as const;
